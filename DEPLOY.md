# 🚀 Инструкция по бесплатному деплою сайта NPAI

Это руководство поможет вам бесплатно разместить ваш рекламный сайт NPAI в интернете.

## 📋 Варианты бесплатного хостинга

### 1. GitHub Pages (Рекомендуется) ⭐
**Преимущества:**
- ✅ Полностью бесплатно
- ✅ Неограниченный трафик
- ✅ Автоматический HTTPS
- ✅ Простая настройка
- ✅ Интеграция с Git

**Ограничения:**
- Публичные репозитории бесплатно
- Приватные репозитории требуют GitHub Pro ($4/мес)

---

### 2. Netlify
**Преимущества:**
- ✅ Очень простой деплой (drag & drop)
- ✅ Автоматический HTTPS
- ✅ CDN по всему миру
- ✅ Бесплатный SSL

**Ограничения:**
- 100 GB трафика/месяц
- 300 минут сборки/месяц

---

### 3. Vercel
**Преимущества:**
- ✅ Отличная производительность
- ✅ Автоматический HTTPS
- ✅ Простой деплой

**Ограничения:**
- 100 GB трафика/месяц

---

## 🎯 Вариант 1: GitHub Pages (Пошаговая инструкция)

### Шаг 1: Создайте аккаунт на GitHub
1. Перейдите на [github.com](https://github.com)
2. Зарегистрируйтесь (если еще нет аккаунта)
3. Подтвердите email

### Шаг 2: Создайте новый репозиторий
1. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
2. Название репозитория: `npai-landing` (или любое другое)
3. Выберите **Public** (для бесплатного GitHub Pages)
4. **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
5. Нажмите **"Create repository"**

### Шаг 3: Инициализируйте Git в вашем проекте

Откройте терминал в папке проекта и выполните:

```bash
# Перейдите в папку проекта
cd "/Users/saulebrimz/Documents/Cursor/NPAI Project/landing-site"

# Инициализируйте Git репозиторий
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: NPAI Landing Site"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/npai-landing.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

**Важно:** Замените `YOUR_USERNAME` на ваш реальный GitHub username!

### Шаг 4: Включите GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите на вкладку **"Settings"** (вверху репозитория)
3. Прокрутите вниз до раздела **"Pages"** (в левом меню)
4. В разделе **"Source"** выберите:
   - Branch: `main`
   - Folder: `/ (root)`
5. Нажмите **"Save"**

### Шаг 5: Настройте корневую папку

GitHub Pages ищет `index.html` в корне репозитория. У вас файлы в папке `landing-site`, поэтому нужно:

**Вариант A: Переместить файлы в корень репозитория (рекомендуется)**

```bash
# В папке landing-site
cd "/Users/saulebrimz/Documents/Cursor/NPAI Project/landing-site"

# Создайте временную папку
mkdir ../temp

# Переместите все файлы
mv * ../temp/
mv .* ../temp/ 2>/dev/null || true

# Вернитесь в корень проекта
cd "/Users/saulebrimz/Documents/Cursor/NPAI Project"

# Переместите файлы из temp в корень
mv temp/* landing-site/
rmdir temp

# Теперь все файлы в landing-site - это и будет корень репозитория
cd landing-site
git add .
git commit -m "Move files to root for GitHub Pages"
git push
```

**Вариант B: Использовать подпапку (проще)**

1. В настройках GitHub Pages выберите:
   - Branch: `main`
   - Folder: `/landing-site`
2. Ваш сайт будет доступен по адресу: `https://YOUR_USERNAME.github.io/npai-landing/landing-site/`

### Шаг 6: Получите ссылку на сайт

После настройки GitHub Pages (может занять 1-2 минуты), ваш сайт будет доступен по адресу:

```
https://YOUR_USERNAME.github.io/npai-landing/
```

или

```
https://YOUR_USERNAME.github.io/npai-landing/landing-site/
```

(в зависимости от выбранного варианта)

---

## 🎯 Вариант 2: Netlify (Самый простой способ)

### Способ 1: Drag & Drop (без Git)

1. Перейдите на [netlify.com](https://netlify.com)
2. Зарегистрируйтесь (можно через GitHub)
3. На главной странице найдите раздел **"Sites"**
4. Перетащите папку `landing-site` в область **"Want to deploy a new site without connecting to Git? Drag and drop your site output folder here"**
5. Netlify автоматически задеплоит ваш сайт!
6. Получите ссылку вида: `https://random-name-12345.netlify.app`
7. Можете изменить имя сайта в настройках

### Способ 2: Через Git (автоматический деплой)

1. Загрузите ваш код на GitHub (см. Вариант 1, шаги 1-3)
2. На Netlify нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **"GitHub"** и авторизуйтесь
4. Выберите ваш репозиторий `npai-landing`
5. Настройки:
   - **Build command:** (оставьте пустым)
   - **Publish directory:** `landing-site` (или `/` если файлы в корне)
6. Нажмите **"Deploy site"**
7. Каждый раз при `git push` сайт будет автоматически обновляться!

---

## 🎯 Вариант 3: Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь (можно через GitHub)
3. Нажмите **"Add New Project"**
4. Импортируйте ваш GitHub репозиторий
5. Настройки:
   - **Framework Preset:** Other
   - **Root Directory:** `landing-site` (или `/`)
6. Нажмите **"Deploy"**
7. Получите ссылку вида: `https://npai-landing.vercel.app`

---

## 🔧 Настройка кастомного домена (опционально)

### Для GitHub Pages:
1. В настройках репозитория → Pages
2. Введите ваш домен в поле **"Custom domain"**
3. Настройте DNS записи у вашего регистратора домена:
   - Тип: `A`
   - Значение: `185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`
   - Или CNAME: `YOUR_USERNAME.github.io`

### Для Netlify/Vercel:
1. В настройках проекта найдите раздел **"Domain"**
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

---

## 📝 Полезные команды Git

После первого деплоя, для обновления сайта используйте:

```bash
cd "/Users/saulebrimz/Documents/Cursor/NPAI Project/landing-site"

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

После `git push`:
- **GitHub Pages** обновится автоматически (может занять 1-2 минуты)
- **Netlify/Vercel** обновятся автоматически (если подключены к Git)

---

## ✅ Чеклист перед деплоем

- [ ] Все файлы находятся в правильных папках
- [ ] Пути к файлам исправлены (относительные пути)
- [ ] `index.html` находится в корневой папке для деплоя
- [ ] Все изображения и стили загружаются корректно
- [ ] Протестирован сайт локально

---

## 🐛 Решение проблем

### Проблема: Сайт не загружается
- Проверьте, что `index.html` находится в корневой папке
- Убедитесь, что все пути относительные (не начинаются с `/` или `C:`)
- Проверьте консоль браузера на ошибки (F12)

### Проблема: Стили не применяются
- Проверьте пути к CSS файлам в `index.html`
- Убедитесь, что файлы `main.css` загружены

### Проблема: JavaScript не работает
- Проверьте консоль браузера на ошибки
- Убедитесь, что пути к JS файлам правильные
- Проверьте, что все файлы загружены

### Проблема: Документация не загружается
- Убедитесь, что файл `NPAI_Project_Description.md` находится в той же папке, что и `index.html`
- Проверьте путь в `scripts/main.js`

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Network вкладку (F12 → Network)
3. Убедитесь, что все файлы загружены на сервер

---

## 🎉 Готово!

После выполнения инструкций ваш сайт будет доступен в интернете бесплатно!

**Рекомендуемый вариант:** GitHub Pages + Netlify (для резервного копирования)

---

*Последнее обновление: 2024*

